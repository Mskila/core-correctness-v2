# Cross-platform AlphaMaster core. Install with:
#   python -m pip install -r requirements.txt -c constraints-core.txt
# Python support: >=3.10,<3.13

torch
numpy
pandas
pyarrow
matplotlib
python-dotenv
loguru
tqdm
fastapi
python-multipart
uvicorn[standard]

# Windows trading adapter dependencies are isolated in their own extra file.
